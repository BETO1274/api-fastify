{
  "name": "orchestrator",
  "version": "1.0.0",
  "description": "Orchestrator + cola — componente transversal del equipo (Azure)",
  "main": "src/server.js",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "node --watch --env-file=.env src/server.js",
    "start": "node src/server.js",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@sinclair/typebox": "^0.34.52",
    "fastify": "^5.12.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.1",
    "pino-pretty": "^13.1.3",
    "vitest": "^4.1.10"
  }
}
