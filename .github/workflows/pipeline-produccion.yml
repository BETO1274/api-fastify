name: Pipeline Producción

on:
  push:
    branches: [produccion]
  pull_request:
    branches: [produccion]

env:
  # Conexión directa (no pooler): evita inconsistencia de caché de esquema justo tras migrar
  DATABASE_URL: ${{ secrets.MIGRATION_DB_URL_PROD }}

permissions:
  contents: read
  packages: write

jobs:
  build-and-test:
    name: Build, pruebas y quality gate (Producción)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Instalar dependencias
        run: npm ci

      - name: Análisis estático (lint)
        run: npm run lint

      - name: Aplicar migraciones pendientes a Supabase Producción
        run: npx supabase db push --db-url "${{ secrets.MIGRATION_DB_URL_PROD }}" --yes

      - name: Ejecutar pruebas con cobertura
        run: npm run test:coverage

      - name: Quality gate — cobertura mínima 85%
        run: npm run check-coverage -- 85

      - name: Build de imagen Docker
        run: docker build -t api-fastify-produccion .

      - name: Smoke test — el contenedor arranca y responde
        run: |
          docker run -d --name smoke-test -p 3000:3000 \
            -e DATABASE_URL="${{ secrets.MIGRATION_DB_URL_PROD }}" \
            -e PORT=3000 \
            api-fastify-produccion

          listo=""
          for intento in $(seq 1 10); do
            if curl -fsS http://localhost:3000/docs > /dev/null 2>&1; then
              listo="si"
              break
            fi
            sleep 1
          done

          docker logs smoke-test
          docker rm -f smoke-test

          if [ -z "$listo" ]; then
            echo "El contenedor nunca respondió en /docs"
            exit 1
          fi
          echo "Contenedor arrancó y respondió correctamente"

      - name: Lint y pruebas del orchestrator
        working-directory: orchestrator
        run: |
          npm ci
          npm run lint
          npm test

      - name: Lint y pruebas del gateway
        working-directory: gateway
        run: |
          npm ci
          npm run lint
          npm test

      - name: Build de imágenes Docker del orchestrator y del gateway
        run: |
          docker build -t orchestrator-produccion ./orchestrator
          docker build -t gateway-produccion ./gateway

      - name: Smoke test — orchestrator y gateway arrancan y se hablan
        run: |
          docker network create smoke-net
          docker run -d --name smoke-orq --network smoke-net orchestrator-produccion
          docker run -d --name smoke-gw --network smoke-net -p 3200:3200 \
            -e ORQUESTADOR_URL=http://smoke-orq:3100 gateway-produccion

          listo=""
          for intento in $(seq 1 15); do
            if curl -fsS http://localhost:3200/api_fastify/articulos/1 -o /tmp/tarea.json 2>/dev/null; then
              listo="si"
              break
            fi
            sleep 1
          done

          docker logs smoke-orq
          docker logs smoke-gw
          docker rm -f smoke-orq smoke-gw
          docker network rm smoke-net

          if [ -z "$listo" ]; then
            echo "El gateway nunca respondió"
            exit 1
          fi
          cat /tmp/tarea.json
          grep -q '"estado":"pendiente"' /tmp/tarea.json
          echo "Gateway y orchestrator arrancaron y la tarea llegó al orquestador"

      - name: Publicar imagen en GitHub Container Registry
        if: github.event_name == 'push'
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
          docker tag api-fastify-produccion ghcr.io/beto1274/api-fastify-produccion:${{ github.sha }}
          docker tag api-fastify-produccion ghcr.io/beto1274/api-fastify-produccion:latest
          docker push ghcr.io/beto1274/api-fastify-produccion:${{ github.sha }}
          docker push ghcr.io/beto1274/api-fastify-produccion:latest
          docker tag orchestrator-produccion ghcr.io/beto1274/orchestrator-produccion:${{ github.sha }}
          docker tag orchestrator-produccion ghcr.io/beto1274/orchestrator-produccion:latest
          docker push ghcr.io/beto1274/orchestrator-produccion:${{ github.sha }}
          docker push ghcr.io/beto1274/orchestrator-produccion:latest
          docker tag gateway-produccion ghcr.io/beto1274/gateway-produccion:${{ github.sha }}
          docker tag gateway-produccion ghcr.io/beto1274/gateway-produccion:latest
          docker push ghcr.io/beto1274/gateway-produccion:${{ github.sha }}
          docker push ghcr.io/beto1274/gateway-produccion:latest

  deploy:
    name: Desplegar en Render (Producción)
    needs: build-and-test
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout del código
        uses: actions/checkout@v4

      - name: Disparar despliegue en Render
        run: curl --fail-with-body -sS -X POST "${{ secrets.RENDER_DEPLOY_HOOK_PROD }}"

      - name: Registrar el Dockerfile desplegado en la rama deploys
        run: |
          cp Dockerfile /tmp/Dockerfile.deployed

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git fetch origin deploys
          git checkout deploys

          mkdir -p deploys
          {
            echo "# Dockerfile desplegado — Producción"
            echo ""
            echo "- Commit: \`${{ github.sha }}\`"
            echo "- Fecha (UTC): $(date -u +"%Y-%m-%d %H:%M:%S")"
            echo ""
            echo '```dockerfile'
            cat /tmp/Dockerfile.deployed
            echo '```'
          } > deploys/dockerfile-produccion.md

          git add deploys/dockerfile-produccion.md
          if ! git diff --cached --quiet; then
            git commit -m "📝 :memo: registrar dockerfile desplegado en produccion"
            git push origin deploys
          else
            echo "Sin cambios en el Dockerfile desplegado"
          fi
