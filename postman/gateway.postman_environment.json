{
  "name": "Gateway Multicloud (AKS)",
  "values": [
    { "key": "base_url", "value": "http://20.245.165.218", "enabled": true, "description": "IP pública del Service 'gateway' en AKS. Si se recrea el Service, cambia — verificar con: kubectl get service gateway -n orchestrator" },
    { "key": "team_api_key", "value": "", "enabled": true, "description": "Completar localmente con la key compartida del equipo (credentials.local.md). Nunca commitear el valor real." }
  ]
}
