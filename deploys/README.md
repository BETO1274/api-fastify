# Registro de despliegues

Esta rama no contiene código de la aplicación — es un registro automático que el pipeline actualiza en cada deploy exitoso, guardando una copia exacta del `Dockerfile` que se usó en ese momento.

- `dockerfile-test.md` — última vez que se desplegó a Test
- `dockerfile-produccion.md` — última vez que se desplegó a Producción
