apiVersion: v1
kind: Service
metadata:
  name: api-fastify
  namespace: api-fastify
spec:
  type: LoadBalancer
  selector:
    app: api-fastify
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
